<% if user_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_user_session_path, :method =>  :delete, :class => "rounded-highlight red") %>
  </li>
<% else %>
  <li>

  <%= link_to('login', new_user_session_path , :class => "rounded-highlight red" , :id => 'login-button')%>
  </li>
<% end %>